info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "nmchien116@gmail.com",
        "password" : "01012006@Chien"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        var aToken = res.body.data.accessToken
        bru.setEnvVar("accessToken", aToken)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
