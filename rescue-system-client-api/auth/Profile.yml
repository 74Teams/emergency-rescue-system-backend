info:
  name: Profile
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/auth/profile"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
